# Zoho SMTP credentials
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
PROJECT_ID=cebu-whitesand-resort
SMTP_USER=reservations@cebu-whitesand-resort.com
SMTP_PASS=<your-smtp-app-password>

# Sender display name override (optional)
# EMAIL_FROM="Cebu Whitesand Resort" <reservations@cebu-whitesand-resort.com>

# Shared secret — must match EMAIL_SERVICE_API_KEY in backend .env
EMAIL_API_KEY=<generate-a-strong-random-secret>

# Resort contact info (used in email footer)
RESORT_PHONE=
RESORT_EMAIL=reservations@cebu-whitesand-resort.com
FACEBOOK_PAGE_URL=

# Admin email (receives new booking + payment alert notifications)
ADMIN_EMAIL=reservations@cebu-whitesand-resort.com

# Frontend URL (for payment links in emails)
CLIENT_URL=https://cebu-whitesand-resort.com

# Backend API URL — used to fetch live social links (phone, Facebook) managed by admin
BACKEND_URL=https://api.cebu-whitesand-resort.com

PROJECT_NAME=Cebu Whitesand Resort
